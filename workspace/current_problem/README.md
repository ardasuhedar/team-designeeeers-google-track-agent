# Current Problem Workspace

Use this directory for local working files that support the active visible Google Track problem.

Examples:

- temporary notes about candidate behavior
- extracted interface summaries
- agent scratch files
- problem-specific experiment outputs that should stay separate from the benchmark repo

The generated Verilog testbench for the active run should still be written to `generated/tb.v` unless you intentionally choose a different path.
